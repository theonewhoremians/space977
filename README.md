# Creator Studio Analytics

A local-only, responsive creator analytics dashboard inspired by the supplied demo.

## Run locally

Requirements: Node.js 22.13 or newer.

```bash
npm install
npm run dev
```

Open `http://localhost:3000` in your browser. The application has no login,
account authentication, remote database, or hosted access requirement.

## Verify

```bash
npm run build
```
